name: First Workflow
on: [workflow_dispatch] # manualy trigger the workflow
jobs:
  first-job:
    runs-on: self-hosted
    permissions:
      security-events: write
    steps:
      - name: SimpleIOT test
        run: |
              cd /usr/testing
              ./script.sh
      - name: SARIF upload - checkout
        uses: actions/checkout@v3
      - name: Print goodbye
        run: echo "Done - bye!"
